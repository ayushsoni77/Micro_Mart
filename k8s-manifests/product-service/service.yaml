apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: product-service-ns
spec:
  selector:
    app: product-service
  ports:
    - port: 3002
      targetPort: 3002
      name: http
